# Improvements

The canonical backlog of incremental improvements lives at
[`docs/agent/improvement-plan.md`](docs/agent/improvement-plan.md).

It contains:

- A **tracking issue** (T-0) ready to paste into GitHub Issues.
- ~16 child issues scoped to single PRs, organized by area (Phaser
  renderer, controller/state, AI, DOM/CSS, service worker, tests, docs).
- Suggested labels, dependencies, and acceptance criteria for each.

See also: [`AGENTS.md`](AGENTS.md) for non-negotiable rules and
[`docs/agent/`](docs/agent/) for topic deep-dives.
